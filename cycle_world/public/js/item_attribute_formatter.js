
frappe.form.link_formatters['Item attribute Value'] = function(value, doc) {
    console.log(value, doc)
    // if(doc.employee_name && doc.employee_name !== value) {
    //     return doc.employee_name + ': ' + value;
    // } else {
    //     return value;
    // }
}